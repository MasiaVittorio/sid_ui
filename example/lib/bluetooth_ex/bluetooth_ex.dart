import 'package:flutter/material.dart';

class BluetoothApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: "Blue Ex",
      home: Scaffold(
        appBar: AppBar(title: Text("Blue Ex"),),
      ),
    );
  }
}